extends ../../components/layout/layout.pug

block variables
    -var title = 'Webpack + Pug'
    -var description = 'Description for Webpack + Pug'
    -var pageClass = 'index-page'

block content
    .index-page__text
        h1.text-block_text-style_h1-title Сайт отеля
        p.text-block_text-style_body Просмотреть страницы сайта или UI kit вы можете, наведя курсор на одно из выпадающих меню сверху.


//- block script
//-     script(type='text/javascript').
//-         const foo = true;
//-         let bar = function() {};
//-         if (foo) {
//-         bar(1 + 5)
//-         }