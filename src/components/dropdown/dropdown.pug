include ../element-name/element-name.pug
include ../input-field/input-field.pug

mixin dropdown(desc, comment, value)
    label.dropdown
        +element-name(desc, comment)
        +input-field("text", "dropdown1", "Сколько гостей", value, "expand_more")