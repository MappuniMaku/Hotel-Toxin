mixin bullet-list(contentArr)
    ul.bullet-list
        -for(let i = 0; i < contentArr.length; i++)
            li.bullet-list__item
                p.bullet-list__text.text-block_text-style_body=contentArr[i]